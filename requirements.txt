# Offline precompute pipeline for the future-climate rarity tool.
# Pinned loosely; tested with the versions resolved in June 2026.
numpy
pandas
scipy
xarray
zarr
gcsfs
dask
intake-esm
aiohttp
requests
# forecast blend (ECMWF IFS HRES open data, GRIB2)
cfgrib
eccodes
